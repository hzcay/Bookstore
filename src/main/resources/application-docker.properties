# Docker Profile Configuration

# Database Configuration (override with docker-compose env vars)
spring.datasource.url=jdbc:postgresql://postgres:5432/bookstore
spring.datasource.username=appuser
spring.datasource.password=123456

# JPA Configuration
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=false
spring.jpa.properties.hibernate.format_sql=false

# Logging Configuration (less verbose for production)
logging.level.com.example.Bookstore=INFO
logging.level.org.springframework.web=INFO
logging.level.org.hibernate.SQL=INFO

# Redis Configuration (use container name)
spring.data.redis.host=redis
spring.data.redis.port=6379

# Email config (same as default)
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=bacdoan52@gmail.com
spring.mail.password=dgkr qjhx mmbu rvwr
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true

# Session Configuration
server.servlet.session.timeout=30m
server.servlet.session.cookie.http-only=true
server.servlet.session.cookie.secure=false
server.servlet.session.cookie.same-site=lax
